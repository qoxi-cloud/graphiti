# Ignore generated certificates
# These are sensitive files that should never be committed

# CA files
ca.key
ca.crt
ca.srl

# Server certificates
server.key
server.crt
server.csr

# Client certificates (mTLS)
client.key
client.crt
client.csr

# Temporary config files
openssl-san.cnf
